# FLOW — תיעוד פרויקט מלא

מסמך זה מרכז את כל מה שהוגדר, נבנה, ואושר עבור FLOW עד כה. מיועד לשמש כ"מקור אמת" — גם לשיחה הזו, וגם אם תעבור לעבוד על הפרויקט בסביבה אחרת (למשל Claude Code).

---

# חלק 1 — הפרומפט הכולל (Master Spec המאוחד)

## 0. זהות וחזון
**שם המוצר: FLOW** (לא DAGapp — שם ישן, לא בשימוש בשום מקום בקוד או בממשק).

FLOW היא לא עוד אפליקציית תקציב. היא **Personal Financial Command Center** — מערכת שמכירה את המשתמש בהדרגה, מבינה את ההתנהלות הפיננסית שלו, ועוזרת לו לקבל החלטות טובות יותר, בלי להרגיש כמו Excel או מערכת בנקאית קרה.

עיקרון-על: **"Simple underneath. Extraordinary on top."** — מאחורי הקלעים המערכת מבצעת חישובים, ניתוחים, השוואות ותובנות; המשתמש רואה חוויה פשוטה בלבד.

## 1. שפה ו-RTL
עברית היא שפת ברירת המחדל של המוצר — כל ממשק המשתמש (כפתורים, כותרות, הודעות, Empty States, שגיאות) בעברית, RTL מלא מתוכנן מלכתחילה (לא תרגום של ממשק אנגלי).

## 2. Design System — "Ocean Calm" (מאושר ונעול)
- **צבעים כהה**: רקע `#071619→#0B2027`, כרטיסים `#0F2830`, Accent `#2B93A8` (כחול-פטרול), Accent2 `#FF9478` (קורל רך), חיובי `#34C77B`, שלילי `#FF6B6B`
- **צבעים בהיר**: רקע `#F5FAFA`, כרטיסים `#FFFFFF`, Accent `#1E7A8C`, Accent2 `#E8734F`
- **טיפוגרפיה**: Heebo (טקסטים/UI), Secular One (כותרות, Wordmark, מספרים גדולים)
- **Wordmark**: `flow` באותיות קטנות + סימן גל מופשט SVG בגרדיאנט פטרול→קורל
- עקרון: Minimalism with personality — Cards, פינות מעוגלות, צללים עדינים, גרדיאנטים עדינים, לא עמוס, לא ילדותי, לא "עוד SaaS גנרי"

## 3. Responsive & Navigation
- מובייל: Bottom Navigation (5 עיקריים: ראשי/כסף/גרפים/יעדים/משימות) + "עוד" לשאר
- דסקטופ: Sidebar מלא (11 פריטים: ראשי, כסף, הוצאות קבועות, חודשים קודמים, גרפים, יעדים, לוח שנה, משימות, מחשבונים, הישגים, הגדרות)
- Safe Area מכובד במובייל (`env(safe-area-inset-*)`), לא צמוד ל-Status Bar
- Touch Targets ≥44px

## 4. Onboarding (זרימה מאושרת)
9 מסכים: Welcome → מי אתה (שם חובה, אימייל אופציונלי) → מטרה מרכזית (צ'יפים) → יעד ראשון (ניתן לדילוג) → הרגלי חיסכון (עם "עוד לא יודע") → תדירות הוצאות + יום משכורת → סף הוצאה חריגה (עם "תן ל-FLOW ללמוד בהדרגה") → תזכורות (טוגל) → סיכום.
- Progress bar עדין, "→ חזרה" בכל מסך חוץ מהראשון
- דילוג מלא אפשרי אך לא דומיננטי ויזואלית
- **כל תשובה נשמרת מיידית** ל-State + localStorage בכל הקשה (לא רק בלחיצת "המשך") — בלי `render()` שגורם לאיבוד פוקוס
- עזיבה באמצע → חזרה מדויקת לאותו שלב עם נתונים קודמים שלמים
- דילוג = Defaults בטוחים בלבד, בלי המצאת נתונים

## 5. Personalization הדרגתי
FLOW לא שואלת הכל ביום הראשון. מידע לא-חיוני (תמונת פרופיל, מטריצת התראות מפורטת, עידון סף חריגה) נאסף בהדרגה בהמשך השימוש, לא ב-Onboarding.

## 6. מודל נתונים פיננסי — עקרונות יסוד
- **מקור אמת יחיד** ב-`data` — שום Entity/חישוב לא משוכפל בין מסכים
- **הכנסות**: רגילה/צדדית/מתנה, כל אחת עם סכום/תאריך/הערה
- **הוצאות**: מתאימות עצמן לתדירות שנבחרה ב-Onboarding — יומי/שבועי = פריטני; סוף חודש = סכום כולל
- **הוצאות קבועות**: מערכת נפרדת (שם/סכום/יום בחודש/קטגוריה/פעיל), **לעולם לא נספרות פעמיים** מול הוצאות רגילות
- **היסטוריה חייבת להישאר היסטורית**: כל חודש שומר `expenseFrequencyAtTime` משלו (לא נקרא מה-Preference הנוכחי), וכל חודש שנסגר "מקפיא" (`fixedExpensesSnapshot`) את ההוצאות הקבועות שהיו פעילות באותו זמן — שינוי/מחיקה של הוצאה קבועה היום **לא** משפיע רטרואקטיבית
- **Month Finalization**: מנגנון שמזהה מעבר לחודש קלנדרי חדש ו"סוגר" את הקודם בבטחה (`lastActiveMonthKey`)
- **שלושה מושגים נפרדים, לעולם לא מתערבבים**:
  - **יתרה נוכחית** = מצטברת על כל החודשים (`Σ(monthIncome-monthExpenses)`), **לא** מופחתת בחיסכון
  - **יתרת החודש** = הכנסות-הוצאות של החודש הנוכחי בלבד
  - **Remaining** = יתרת החודש **פחות** חיסכון החודש

## 7. חיסכון ויעדים
- חיסכון: רשומות בתוך אותו Month record (לא Entity מקביל), חיסכון מצטבר, ממוצע חודשי
- יעדים מרובים: שם, אימוג'י (בורר מקוטלג — טיולים/רכבים/תכשיטים/טכנולוגיה/בית/לימודים/קניות/כסף), סכום יעד, סכום התחלתי, תאריך יעד, Progress
- אבני דרך ויזואליות: 25%→🎯 50%→💪 75%→🔥 90%→🚀 100%→🎉, עם חגיגה עדינה (לא ילדותית) בהשלמה, **פעם אחת בלבד**
- **סטריק חיסכון**: חודשים רצופים עם חיסכון>0, נספר אחורה מהחודש הנוכחי, עוצר בפער
- **הישגים**: 7 הישגים נגזרים בזמן אמת מהנתונים בפועל (לא שדה שאפשר "לזייף")

## 8. היסטוריה (Read-Only)
מסך "חודשים קודמים": רשימת חודשים → פירוט מלא לכל חודש (מציג בדיוק את אותם חישובים שהיו מוצגים אז, לפי `expenseFrequencyAtTime` ו-`fixedExpensesSnapshot` **של אותו חודש**) → השוואה לחודש הקודם **הקיים בפועל** (מדלגת על פערים) → ניווט קדימה/אחורה. שכבת תצוגה בלבד — לא כותבת נתונים.

## 9. עדיין לא ממומש (מוגדר במפרט המקורי, ממתין לתור)
- **גרפים אינטראקטיביים** (הכנסות מול הוצאות, חיסכון לאורך זמן, שינוי יתרה, מקורות הכנסה) עם Drill-down, Filters (חודש/3/6/שנה/הכל), Tooltips מדויקים
- **משימות + לוח שנה מלא** עם תאריך עברי אמיתי, "חשוב להיום" בפתיחה (פעם אחת לפתיחה)
- **מחשבון מרכזי** אחד עם כל המצבים (חיסכון/יעד/חיסכון חודשי/אחוז/יתרה/תשואה/מטבע) + מחשבון רגיל **אמיתי** עם מקלדת (AC/±/%/÷×−+/ספרות/=), "השתמש בנתונים שלי"
- **התראות**: In-App בלבד בשלב זה (תזכורת משכורת, תזכורת סוף חודש) — לא Push (ראה סעיף 11)
- **הגדרות**: פרופיל, העדפות פיננסיות, תדירות עדכון (ניתנת לשינוי), התראות, Theme, ניהול נתונים (Export/Delete)
- Money Animations מלאות (Count-Up כבר קיים; אנימציות "רצף" מורכבות יותר טרם)

## 10. מגבלות סביבה (הוסכם במפורש)
- **אין שרת/Backend** — כל השמירה ב-`localStorage` בדפדפן בלבד. אין סנכרון בין מכשירים.
- **אין Push Notifications אמיתיות** — רק In-App, מוצג כשה-FLOW פתוחה. הוסכם כמקובל לשלב זה.
- **אין הרשמה/Auth אמיתיים** — פרופיל מקומי (שם, אימייל טקסטואלי) בלבד.
- הארכיטקטורה נשמרת **פתוחה** להרחבה עתידית (iOS/Android native, Push, Backend, Cloud Sync, Auth) — בלי לבנות את זה עכשיו.

## 11. עקרונות עבודה (איך התהליך מתנהל)
- כל Phase מקבל **Work Prompt** מפורט (מטרה/מודול/מצב קיים/מה נבנה/מה לא משתנה/Dependencies/Data Model/UX/Acceptance Criteria/Tests) — **מאושר לפני כל שורת קוד**
- שום החלטה מוצרית משמעותית לא מתקבלת חד-צדדית — עצירה ושאלה מרוכזת כשיש אי-ודאות אמיתית
- כל Phase: תכנון → בנייה → בדיקות Data → בדיקות Real Browser (לחיצות/מגע אמיתיים, לא רק סינטקס) → Regression מול **כל** מה שאושר קודם → דוח מלא → אישור מפורש → רק אז ה-Phase הבא
- Design System, Onboarding, וניווט "קפואים" ברגע שאושרו — לא משתנים בלי סיבה מפורשת

---

# חלק 2 — מה שנעשה בפועל, כולל החלטות, תיקונים ובעיות

## Phase 1 — Foundation ✅
נבנה `flow.html` מאפס לגמרי (לא המשך קוד של DAGapp). Design System Ocean Calm יושם במלואו, ניווט (Sidebar+Bottom Nav) עם מסכי Placeholder מינימליים, Dark/Light Mode, Safe Area.
**החלטה מרכזית**: המיתוג לא ננעל באופן בלתי הפיך עד שנבדק בפועל בדפדפן/מכשיר — הוצג לאישור עם 3 כיווני צבע חלופיים לפני הבחירה הסופית ב-Ocean Calm.

## Phase 2 — Onboarding & Personalization ✅
9 מסכים לפי המסלול המאושר. שדה "הוצאה חריגה" נוסף (לא היה בתכנון הראשוני — נדרש ותוקן לפני האישור).
**באג אמיתי שנמצא ותוקן**: מנגנון השמירה המקורי שמר תשובות רק בלחיצת "המשך" (טיוטה זמנית) — המשתמש דרש שמירה על **כל הקשה בודדת**. נבנה מחדש: כל שדה כותב ישירות ל-`data`+`localStorage` בזמן אמת (`bindLive`), בלי `render()` שהיה גורם לאיבוד פוקוס. אומת ב-20 בדיקות Data (headless) + Real Browser.
**באג קריטי שני**: בשלב "מה הכי חשוב לך" — ה-Chips שמרו לשדה `motivation` בעוד כל שאר הקוד קרא מ-`primaryMotivation`. תוצאה: **המשתמש נתקע** לגמרי במסך הזה, נראה כאילו שום כפתור לא עובד. אותר ותוקן, אומת מחדש עם Playwright כולל **מגע אמיתי (touch) בהדמיית iPhone 13**, לא רק קליק עכבר.
**באג שלישי**: כפתור "איפוס נתונים" השתמש ב-`confirm()` המובנה של הדפדפן — חסום בשקט ב-iOS כשהאפליקציה מותקנת כאייקון למסך הבית. הוחלף בחלון אישור מותאם בתוך האפליקציה.

## Phase 3 — Finance ✅
הכנסות (רגיל/צדדי/מתנה), הוצאות מתאימות-תדירות, הוצאות קבועות. **הליבה הארכיטקטונית**: `expenseFrequencyAtTime` + `fixedExpensesSnapshot` ברמת כל Month, מנגנון Month Finalization מלא (`lastActiveMonthKey`), טיפול מפורש בחודש-בדיעבד (נסגר מיד עם הקירוב הכי טוב הזמין). הוחלט ואושר: שינוי הגדרות הווה **לעולם** לא משפיע רטרואקטיבית על חודשים סגורים.
**23 בדיקות Data** (כולל: עריכת/מחיקת הוצאה קבועה אחרי סגירה, שינוי תדירות אחרי שנוצרו חודשים, חודש-בדיעבד, סכומי 0) + **Real Browser** (דסקטופ+מגע) — הכל עבר. סבב אימות נוסף יזום ע"י המשתמש בדק במפורש פער-של-כמה-חודשים בין פתיחות ו"דליפה" של הוצאה קבועה חדשה אחורה בזמן — גם זה עבר (7 בדיקות נוספות).

## Phase 4 — Savings & Goals ✅
בלוק חיסכון בתוך מסך "כסף", מסך יעדים מלא (בורר אימוג'י מקוטלג, Progress, אבני דרך, חגיגה), מסך הישגים (7 הישגים נגזרים), סטריק. `Remaining` עודכן לחשב חיסכון אמיתי.
**החלטה מוצרית שהובאה לאישור מפורש**: `currentLiquidBalance` **לא** מופחת בחיסכון — חיסכון הוא "הקצאה מוצהרת" נפרדת למעקב/יעדים, לא הוצאה שמקטינה יתרה נזילה. אושר במפורש, ואומת שהוספת הפקדת חיסכון גדולה (10,000₪) לא הזיזה את `currentLiquidBalance` אפילו שקל.
**באג אמיתי שנמצא ותוקן**: חלון חגיגת 100% נסגר רק בלחיצה מדויקת על הכפתור או על שולי הרקע — לחיצה על גוף הכרטיס (טבעי למשתמש) לא עשתה כלום. תוקן כך שכל לחיצה בתוך החלון סוגרת אותו.
**26 בדיקות Data + 15 Real Browser Desktop + 7 Real Browser Touch** — הכל עבר, כולל 76 הבדיקות הקודמות ללא שינוי.

## Phase 5 — History ✅
מסך "חודשים קודמים" הפך משכבת-תצוגה ריקה למסך אמיתי: רשימה, פירוט מלא לכל חודש (קורא **לאותן פונקציות בדיוק** של Phase 3/4 — לא נוסחה כפולה), השוואה לחודש קודם קיים, ניווט. שכבת קריאה בלבד, ללא שינוי Data Model.
**14 בדיקות Data + 7 Real Browser Desktop + 5 Real Browser Touch** — הכל עבר. אומת במפורש שרינדור מסך ההיסטוריה לא משנה שום ביט ב-`data.months` (Read-Only אמיתי, לא רק בכוונה).

## מתודולוגיית בדיקות שהתפתחה לאורך הפרויקט
1. **Data/Logic** — Test Harness ב-Node שמריץ את קוד ה-App האמיתי (לא סימולציה חיצונית) עם DOM מבוים
2. **Real Browser** — Playwright, דפדפן Chromium אמיתי headless: קליקים אמיתיים בדסקטופ **וגם** מגע אמיתי (`tap()`) בהדמיית iPhone 13 מלאה — לא רק שינוי רוחב חלון
3. **Regression מצטבר** — כל סוויטת בדיקות מ-Phase קודם רצה שוב, ללא שינוי, בכל Phase חדש (הגיע ל-95+ בדיקות אוטומטיות עד Phase 5)
4. שני הבאגים הקריטיים ביותר (Onboarding תקוע, חגיגה שלא נסגרת) **לא** נתפסו ע"י בדיקות Syntax/Data בלבד — רק Real Browser עם אינטראקציה אמיתית חשף אותם. זה הוביל להקפדה קבועה על Real Browser testing מ-Phase 3 ואילך.

---

# חלק 3 — מה עוד לא נעשה

לפי המפרט המקורי, בסדר שהוגדר:

1. **Analytics / גרפים** — הכנסות מול הוצאות, חיסכון לאורך זמן, שינוי יתרה, מקורות הכנסה. אינטראקטיביים, עם Drill-down לחודש, Filters לפי טווח זמן, Tooltips מדויקים. **לא קיים כלל עדיין** — מסך "גרפים" עדיין Placeholder.
2. **Productivity** — משימות ("חשוב להיום"), לוח שנה מלא עם ניווט בין חודשים, תאריך עברי אמיתי, Popup פתיחה יומי. **לא קיים** — מסכי "לוח שנה" ו"משימות" עדיין Placeholder.
3. **Calculators** — מחשבון מרכזי עם כל 8 המצבים (כולל מחשבון רגיל אמיתי עם מקלדת), "השתמש בנתונים שלי". **לא קיים** — מסך "מחשבונים" עדיין Placeholder.
4. **Notifications** — מערכת תזכורת משכורת ותזכורת סוף-חודש (In-App). קיים רק הטוגל הכללי מה-Onboarding, אין עדיין לוגיקה שמפעילה תזכורות בפועל.
5. **Settings המלא** — פרופיל, עריכת העדפות פיננסיות, שינוי תדירות עדכון הוצאות (`preferences.expenseFrequency` נקבע ב-Onboarding אך **אין עדיין דרך לשנות אותו** אחר כך בממשק), ניהול נתונים (Export/Import/Delete), ניהול Theme מרוכז. מסך "הגדרות" עדיין Placeholder.
6. **Polish** — ליטוש אנימציות (מעבר ל"Money Moment" מלא — עדכון מחובר של יתרה/גרף/יעד/Emoji בפעולה אחת), Empty/Error States מלאים, Micro-interactions נוספים.
7. **QA רוחבי** — Accessibility מלא (Contrast, Focus States, Screen Reader), בדיקות ביצועים.
8. **Distribution Readiness** — הכנה ל-PWA אמיתי (manifest, Service Worker), ולא כל שכן ל-App Store/Google Play — לא התחיל בכלל, ולפי ההחלטה המפורשת לא יתחיל בלי בקשה מפורשת.
9. **יזואלים ליעדים** (תמונה/GIF/Lottie) — הוחלף באימוג'י בלבד, כפשרה טכנית מוצהרת מלכתחילה.

---

# חלק 4 — דעתי הכנה על האפליקציה

**מה עובד טוב באמת:**
הארכיטקטורה של השכבה ההיסטורית (Snapshot + `expenseFrequencyAtTime`) היא הדבר הכי חזק כאן — זו בעיה אמיתית שרוב אפליקציות תקציב "מהירות" פשוט מתעלמות ממנה, וכאן היא נפתרה נכון מההתחלה ונבדקה לעומק. משטר הבדיקות (Data + Real Browser + Regression מצטבר) גם חריג ברמת הרצינות שלו לפרויקט בסדר גודל הזה — ושני הבאגים האמיתיים שנתפסו (Onboarding תקוע, חגיגה תקועה) הם בדיוק סוג הדברים שהיו "עוברים בשקט" בלי המשטר הזה.

**מה שמדאיג אותי, בכנות:**
- **קובץ HTML יחיד, כמעט 2,000 שורות (~79KB)** — עובד מצוין עכשיו, אבל עם עוד 6-7 Phases שנותרו (גרפים, לוח שנה, מחשבונים, הגדרות...) הקובץ הזה יגדל משמעותית. בשלב מסוים העריכה בתוכו (גם שלי, גם שלך אם תעביר ל-Claude Code) תיהפך מסורבלת יותר. שווה לשקול בעתיד פיצול לקבצים/מודולים — לא דחוף עכשיו, אבל כדאי לזכור.
- **אני לא רואה את האפליקציה** — כל הבדיקות שלי (כולל Real Browser) בודקות התנהגות ומבנה DOM, לא איך זה *נראה* בפועל. גדלים, ריווח, האם הצפיפות במובייל נוחה בעין — זה נשאר לגמרי בידיים שלך לבדוק. אני סומך על כך שתמשיך לתפוס דברים כמו שתפסת עד עכשיו (בקשות ה"תקטין", "תגדיל" הקודמות היו בדיוק זה).
- **אין עדיין שום דרך לשנות `expenseFrequency` אחרי ה-Onboarding** — זו לא בעיה טכנית, פשוט Settings עוד לא נבנה, אבל זה אומר שמי שבחר "יומי" בטעות תקוע איתו עד Phase שיבנה את זה.
- מבנה "אישור לפני כל שורת קוד" עובד מצוין לאיכות, אבל הוא איטי — אם המטרה היא להגיע למוצר שלם, שווה לחשוב אילו Phases הבאים (למשל גרפים, מחשבונים) יכולים להתקדם מהר יותר בלי לאבד את רמת הבדיקה.

בסך הכל — זה לא "עוד דמו יפה", יש כאן בסיס נתונים אמיתי ובדוק. השאלה הפתוחה היחידה מבחינתי היא קצב: 5 Phases תוך כדי משטר בדיקות כזה זה הישג, אבל יש עוד דרך.
